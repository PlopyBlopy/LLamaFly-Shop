export enum ProfileCommonActions {
  PROFILE = "profile", //routing
  SETTINGS = "settings", //routing
  LOGOUT = "logout", //logc
}

export enum ProfileUnAuthorizedActions {
  REGISTER = "register",
  LOGIN = "login",
}

export enum ProfileSellerActions {
  MY_PRODUCTS = "my-products", //routing
  ADD_PRODUCT = "add-product", //routing
}

export enum ProfileCustomerActions {
  ORDER_HISTORY = "order-history",
}
